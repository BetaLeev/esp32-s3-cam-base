# ESP32-S3 硬件接线文档

本文档详细说明 ESP32-S3 智能水泵控制系统的硬件接线方案。

---

## 目录

1. [系统架构](#1-系统架构)
2. [电源系统](#2-电源系统)
3. [TB6612电机驱动接线](#3-tb6612电机驱动接线)
4. [传感器接线](#4-传感器接线)
5. [舵机接线](#5-舵机接线)
6. [完整接线图](#6-完整接线图)
7. [常见问题排查](#7-常见问题排查)

---

## 1. 系统架构

```
┌─────────────────┐
│   ESP32-S3      │
│  (主控制器)      │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼──┐  ┌──▼────┐  ┌────────┐
│ TB6612│  │ DHT11 │  │ SG90   │
│ (水泵)│  │(温湿度)│  │(舵机)  │
└───────┘  └───────┘  └────────┘
```

---

## 2. 电源系统

### 电源需求

| 模块 | 电压 | 电流 | 电源来源 |
|------|------|------|----------|
| ESP32-S3 | 3.3V / 5V | ~500mA | USB或外部电源 |
| TB6612 逻辑 | 2.7-5.5V | ~20mA | ESP32 3.3V |
| TB6612 电机 | 5-13.5V | 按电机 | **外部电源** |
| 水泵电机 | 5-12V | 按电机 | TB6612 VMOT |
| SG90 舵机 | 4.8-6V | ~250mA | 外部电源 |
| DHT11 | 3.3-5V | ~2.5mA | ESP32 3.3V |

### 电源接线原则

```
⚠️ 重要规则：
1. 所有GND必须共地
2. VMOT（电机电源）和VCC（逻辑电源）可以分开供电
3. 外部电源负极 → TB6612 GND → ESP32 GND 必须连接在一起
```

### 电源连接图

```
外部电源 (5-12V)              ESP32-S3
     │                           │
     │                     ┌─────┴─────┐
     │                     │   3.3V    │ ← VCC (逻辑)
     │                     │   5V      │ ← VCC (备用)
     │                     │   GND     │ ← 共地
     │                     └───────────┘
     │
     ├─── + ──► VMOT              TB6612模块
     │     (电机电源)                    │
     └─── - ──► GND ──────────────► GND (必须共地)
```

---

## 3. TB6612电机驱动接线

TB6612是一款双H桥电机驱动芯片，支持两路独立电机控制。

### 引脚说明

| 引脚 | 功能 | 说明 |
|------|------|------|
| VMOT | 电机电源 | 5-13.5V，接外部电源正极 |
| VCC | 逻辑电源 | 2.7-5.5V，接3.3V或5V |
| GND | 接地 | 所有GND必须连在一起 |
| STBY | 待机控制 | **必须接高电平(3.3V)才能工作** |
| PWMA | A通道PWM | 控制A通道电机速度 |
| AIN1 | A通道方向1 | 与AIN2配合控制方向 |
| AIN2 | A通道方向2 | 与AIN1配合控制方向 |
| AO1 | A通道输出1 | 接电机线1 |
| AO2 | A通道输出2 | 接电机线2 |
| PWMB | B通道PWM | 控制B通道电机速度（未使用） |
| BIN1 | B通道方向1 | 未使用 |
| BIN2 | B通道方向2 | 未使用 |
| BO1 | B通道输出1 | 未使用 |
| BO2 | B通道输出2 | 未使用 |

### A通道接线（水泵 - 当前使用）

```
ESP32-S3                    TB6612 (A通道)
--------                    -------------
GPIO 1  ──────────────────►  PWMA    (PWM速度控制)
GPIO 2  ──────────────────►  AIN1    (方向控制)
GPIO 42 ──────────────────►  AIN2    (方向控制)
3.3V   ──────────────────►  VCC     (逻辑电源)
3.3V   ──────────────────►  STBY    (待机使能，必须高)
GND    ──────────────────►  GND     (共地)

外部电源 (5-12V)
     │
     ├─── + ──► VMOT    (电机电源输入)
     │
     └─── - ──► GND     (电源负极)

TB6612                    水泵/潜水泵
------                    ----------
AO1   ──────────────────►  电机线1 (任意)
AO2   ──────────────────►  电机线2 (任意)
```

### B通道接线（预留 - 当前未使用）

B通道可以用于控制第二个电机/水泵，接线方式与A通道相同：

```
ESP32-S3                    TB6612 (B通道)
--------                    -------------
GPIO X  ──────────────────►  PWMB    (PWM速度控制)
GPIO X  ──────────────────►  BIN1    (方向控制)
GPIO X  ──────────────────►  BIN2    (方向控制)
                             BO1     ──► 电机2线1
                             BO2     ──► 电机2线2
```

### 电机方向控制逻辑

| AIN1 | AIN2 | 电机状态 |
|------|------|----------|
| 0 | 0 | 停止 |
| 1 | 0 | 正转 |
| 0 | 1 | 反转 |
| 1 | 1 | 停止（刹车） |

---

## 4. 传感器接线

### DHT11 温湿度传感器

| ESP32-S3 | DHT11模块 |
|----------|-----------|
| GPIO 4 | DATA |
| 3.3V | VCC |
| GND | GND |

```
ESP32-S3              DHT11
--------              -----
GPIO 4  ────────────► DATA
3.3V   ────────────► VCC
GND    ────────────► GND
```

> **注意**：DHT11使用单总线协议，只需要一根数据线即可同时传输温度和湿度数据。

### 热敏电阻 (NTC)

| ESP32-S3 | 模块 | 说明 |
|----------|------|------|
| GPIO 5 | ADC | 模拟输入 |
| 3.3V | VCC | 电源 |
| GND | GND | 接地 |

### 光敏电阻 (LDR)

| ESP32-S3 | 模块 | 说明 |
|----------|------|------|
| GPIO 3 | ADC | 模拟输入 |
| 3.3V | VCC | 电源（通过光敏电阻） |
| GND | GND | 接地 |

---

## 5. 舵机接线

### SG90 舵机

| ESP32-S3 | SG90舵机 | 线色（常见） |
|----------|----------|--------------|
| GPIO 48 | Signal | 橙色/黄色 |
| 3.3V 或 5V | VCC | 红色 |
| GND | GND | 棕色/黑色 |

```
ESP32-S3              SG90舵机
--------              --------
GPIO 48 ─────────────► SIGNAL (控制信号)
3.3V/5V ────────────► VCC   (电源)
GND    ────────────► GND   (接地)
```

> **注意**：舵机需要单独供电，不要从ESP32直接取电给舵机，以免电流不足。

---

## 6. 完整接线图

```
┌──────────────────────────────────────────────────────────────────┐
│                         ESP32-S3 开发板                          │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ GPIO 1 │ │ GPIO 2 │ │ GPIO 3 │ │ GPIO 4 │ │ GPIO 5 │        │
│  │   │    │ │   │    │ │   │    │ │   │    │ │   │    │        │
│  │   │    │ │   │    │ │   │    │ │   │    │ │   │    │        │
│  └───┼────┘ └───┼────┘ └───┼────┘ └───┼────┘ └───┼────┘        │
│      │          │          │          │          │              │
└──────┼──────────┼──────────┼──────────┼──────────┼──────────────┘
       │          │          │          │          │
       │          │          │          │          │
       ▼          ▼          ▼          ▼          ▼
    ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐
    │PWMA │   │AIN1 │   │     │   │DATA │   │     │
    │     │   │AIN2 │   │     │   │     │   │     │
    │ VCC │   │STBY │   │     │   │VCC  │   │VCC  │
    │ GND │   │ GND │   │     │   │ GND │   │ GND │
    └─────┘   └─────┘   └─────┘   └─────┘   └─────┘
       │          │                              │
       │          │                              │
    ┌──▼──────────▼──────────────────────────┐  │
    │          TB6612 驱动模块                │  │
    │  VMOT ───────────────────┐              │  │
    │  AO1  ──────────► 水泵1  │              │  │
    │  AO2  ──────────► 水泵1  │              │  │
    │  BO1  ──────────► [未用] │              │  │
    │  BO2  ──────────► [未用] │              │  │
    └───────────────────────────┘              │
                                             │
    ┌────────────────┐              ┌────────┴───┐
    │   DHT11        │              │   热敏电阻  │
    │   温湿度传感器  │              │   (ADC)    │
    └────────────────┘              └────────────┘

外部电源 (5-12V)
     │
     ├─── + ──► VMOT (TB6612)
     └─── - ──► GND (所有GND共地)
```

---

## 7. 常见问题排查

### 水泵不转

1. **检查STBY引脚**：必须接高电平(3.3V)
2. **检查GND连接**：所有GND必须共地
3. **检查VMOT电压**：确认外部电源电压正确
4. **检查PWM信号**：用万用表测量PWMA引脚电压

### 水泵只动一下不转

**问题原因**：启动电流不足

**解决方案**：
- 系统已内置软启动功能
- 启动时会先以100%速度运行150ms，再降到目标速度
- 如果仍有问题，检查电机负载是否过大

### 舵机不转

1. **检查电源**：舵机需要单独供电，不要从ESP32取电
2. **检查信号线**：GPIO 48连接是否正确
3. **检查PWM频率**：SG90需要50Hz信号

### DHT11读数异常

1. **检查接线**：确认DATA、VCC、GND连接正确
2. **检查库文件**：确认使用了正确的DHT驱动库
3. **延迟问题**：DHT11读取间隔至少2秒

### 温湿度显示 --

1. **传感器未初始化**：检查代码中DHT11初始化是否成功
2. **引脚配置错误**：确认GPIO 4配置正确

---

## GPIO引脚分配表

| GPIO | 功能 | 备注 |
|------|------|------|
| GPIO 1 | PWMA | 电机PWM输出 |
| GPIO 2 | AIN1 | 电机方向控制 |
| GPIO 3 | 光敏电阻ADC | 模拟输入 |
| GPIO 4 | DHT11 DATA | 单总线通信 |
| GPIO 5 | 热敏电阻ADC | 模拟输入 |
| GPIO 42 | AIN2 | 电机方向控制 |
| GPIO 48 | SG90舵机 | PWM控制 |
| GPIO 38 | SD_MMC CMD | TF卡（可选） |
| GPIO 39 | SD_MMC CLK | TF卡（可选） |
| GPIO 40 | SD_MMC D0 | TF卡（可选） |

---

## 更新日志

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-07-11 | v1.0 | 初始文档创建 |
| 2026-07-11 | v1.1 | 添加软启动说明，优化接线图 |

---

**文档版本**: v1.1
**最后更新**: 2026-07-11
